#关于shell的学习

#关于“=”
1, “=”两边不能直接接空格符
2, 变量内容有空格符可使用双引号或单引号将变量内容结合起来

#关于变量
1,通常大写字符为系统默认变量,自行配置变量可以使用小写字符

# `` (数字1旁边的按键)
``里的命令先执行再将结果作为外部的输入
e.g ls -l `locate crontab`
容易打错、看错所以常用$来代替
e.g version=$(uname -r) 相当于 version=`uname -r`

#查看变量环境
env 或者 export
